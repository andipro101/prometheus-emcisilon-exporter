apiVersion: v1
kind: Service
metadata:
  labels:
    app: isilon-exporter
  name: isilon-exporter
  namespace: monitoring
spec:
  ports:
  - name: metrics
    port: 9300
    protocol: TCP
    targetPort: metrics
  selector:
    app: isilon-exporter
